[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "invesalius_rs"
version = "0.1.0"
description = "Rust extension for InVesalius"
requires-python = ">=3.8"

[tool.maturin]
python-source = "python"
module-name = "invesalius_rs._native"
